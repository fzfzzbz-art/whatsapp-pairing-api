services:
  - type: web
    name: whatsapp-pairing-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.x
      - key: BOT_TOKEN
        sync: false # سيطلب منك إدخاله يدوياً في الموقع لزيادة الأمان
